# Code generated by shipbuilder init 1.21.0. DO NOT EDIT.

name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
    outputs:
      version: ${{ steps.version.outputs.version }}
      version-major: ${{ steps.version.outputs.version-major-only }}
      version-minor: ${{ format('{0}.{1}', steps.version.outputs.version-major-only, steps.version.outputs.version-minor-only) }}
      version-patch: ${{ format('{0}.{1}.{2}', steps.version.outputs.version-major-only, steps.version.outputs.version-minor-only, steps.version.outputs.version-patch-only) }}
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Generate next version
      id: version
      uses: conventional-actions/next-version@v1
    - name: Setup .netrc
      uses: conventional-actions/setup-netrc@v1
    - name: Setup Go
      uses: actions/setup-go@v3
      with:
        go-version: "1.18"
        cache: true
    - name: Extract Go module metadata
      uses: conventional-actions/go-metadata@v1
      id: metadata
    - name: Setup Go version
      uses: conventional-actions/go-version@v1
      with:
        version: ${{ steps.version.outputs.version }}
        package: ${{ steps.metadata.outputs.module }}
        variable: Version
    - name: Build Go code
      uses: conventional-actions/go-build@v1
      with:
        ldflags: '-s -w'
        package: |
          ./cmd/ketch
        platforms: |
          linux/amd64
          linux/arm64
          darwin/amd64
          darwin/arm64
          windows/amd64
    - uses: actions/upload-artifact@v3
      with:
        name: docker
        path: docker

  package-ketch:
    name: Package ketch
    needs: build
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
    permissions:
      contents: read
      packages: write
    steps:
    - uses: conventional-actions/docker-build@v1
      with:
        name: ketch-cli
        snyk-token: ${{ secrets.SNYK_AUTH_TOKEN }}
        version-major: ${{ needs.build.outputs.version-major }}
        version-minor: ${{ needs.build.outputs.version-minor }}
        version-patch: ${{ needs.build.outputs.version-patch }}

  release:
    name: Release
    if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    needs:
    - build
    - package-ketch
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Create Release
      uses: conventional-actions/create-release@v1
      with:
        tag_name: ${{ needs.build.outputs.version }}
        artifacts: '*'

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}
